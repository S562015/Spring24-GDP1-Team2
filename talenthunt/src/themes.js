import { createTheme } from "@mui/material/styles";

export const globalTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#5319AC",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
